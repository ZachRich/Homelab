users:
  admin:
    displayname: Administrator
    password: {{ authelia_admin_password | default('changeme123') | password_hash('bcrypt') }}
    email: {{ authelia_admin_email | default('admin@homelab.local') }}
    groups:
      - admins
      - users
  user:
    displayname: Regular User
    password: {{ authelia_user_password | default('userpass123') | password_hash('bcrypt') }}
    email: {{ authelia_user_email | default('user@homelab.local') }}
    groups:
      - users